# Claude Code adapter

Use `AGENTS.md` as the behavioral contract for this repository and `.project-brain/` as canonical persistent project memory.

At session start, prefer `brain index` followed by `brain context "<task>"` instead of recursively reading the whole repository. Use intents and semantic batches for multi-step work. Escalate security, database migration, breaking API, production incident, and architecture decisions to the strongest available model.
